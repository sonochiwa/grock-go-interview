# Shape Calculator

Определи интерфейс `Shape` с методами `Area() float64` и `Perimeter() float64`.

Реализуй для:
- `Circle{Radius float64}`
- `Rectangle{Width, Height float64}`

Напиши функцию `TotalArea(shapes ...Shape) float64` — сумма площадей всех фигур.
