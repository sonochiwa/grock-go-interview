# Optimize Me

Дана функция `ProcessRecords` — намеренно неоптимальная. Оптимизируй её минимум в 3x.

Проблемы в коде:
- string конкатенация в цикле
- нет предаллокации слайсов
- fmt.Sprintf для простых конверсий
- ненужные аллокации

Запусти `go test -bench=.` до и после для сравнения.
